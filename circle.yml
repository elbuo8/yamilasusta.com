machine:
  node:
    version: 6.1.0
  services:
    - docker

dependencies:
  override:
    - docker build --rm=false -t elbuo/yamilasusta.com:latest .

test:
  override:
    - docker run elbuo/yamilasusta.com:latest npm i mocha && mocha

deployment:
  master:
    branch: master
    owner: elbuo8
    commands:
      - sh deployment/deploy-master.sh
